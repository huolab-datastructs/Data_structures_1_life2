#include <iostream>
using namespace std;

/* This could be used if the particular c++ compiler doesn't include bool as a type.
typedef int bool;
const bool false = 0;
const bool true = 1;
*/

bool user_says_yes();
